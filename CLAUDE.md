# predicting_vehicle_purchases

## Agent skills

### Issue tracker

Issues live as GitHub issues in `ronaldoi9/predicting_vehicle_purchases`, managed with the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles, each label string equal to its name. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context — `CONTEXT.md` at the repo root, ADRs in `docs/adr/`. See `docs/agents/domain.md`.
